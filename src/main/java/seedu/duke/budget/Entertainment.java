package seedu.duke.budget;

public class Entertainment extends GoodsAndServices {
    /**
     * Constructor that creates a new Entertainment object with its cost initialised.
     *
     * @param price the cost of entertainment the user is planning to spend.
     */
    public Entertainment(int price) {
        super(price);
    }
}
