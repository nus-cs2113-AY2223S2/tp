package seedu.duke.budget;

public class Food extends GoodsAndServices {
    /**
     * Constructor that creates a new Food object with its cost initialised.
     *
     * @param price the cost of food the user is planning to spend.
     */
    public Food(int price) {
        super(price);
    }

}
