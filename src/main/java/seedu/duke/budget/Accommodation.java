package seedu.duke.budget;

public class Accommodation extends GoodsAndServices {
    /**
     * Constructor that creates a new Accommodation object with its cost initialised.
     *
     * @param price the cost of accommodation the user is planning to spend.
     */
    public Accommodation(int price) {
        super(price);
    }

}
