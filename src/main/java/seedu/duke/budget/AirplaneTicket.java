package seedu.duke.budget;

public class AirplaneTicket extends GoodsAndServices {
    /**
     * Constructor that creates a new AirplaneTicket object with its cost initialised.
     *
     * @param price the cost of the airplane ticket the user is planning to spend.
     */
    public AirplaneTicket(int price) {
        super(price);
    }
}
